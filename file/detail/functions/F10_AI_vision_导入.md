# F10 · AI vision 导入（图片 / 扫描件）— M4

> 本文件由 `scripts/split_docs.py` 从《项目功能.md》拆分（2026-08-08 路由化定案）；真相源 = 本文档。

### F10 · AI vision 导入（图片 / 扫描件）— M4
- **需求**：从图片或扫描版 PDF 用多模态模型抽取结构化简历。
- **实现框架**：复用 F6 三步向导；主进程统一 `callAIVision`（Gemini `inlineData` / OpenAI·豆包 `image_url` 分支）；**双向守卫**：非视觉模型服务端 400 拒绝 + 客户端禁用（纪律见《项目规范.md》4.4）。
- **用户视角**：「导入 → 从图片 / 扫描 PDF」→ 多模态识别 → 同一套三步向导；选了不支持视觉的模型 → 选项直接禁用并提示「需选 Gemini / OpenAI / 豆包」。
