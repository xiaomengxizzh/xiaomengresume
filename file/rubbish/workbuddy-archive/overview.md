# UI/配色五次定案 · 任务概览（2026-08-06）

## 做了什么
学习用户提供的新参考图 `file/rubbish/UI示例_参考图.html` 的 UI 设计，并将**全新 4 主题配色方案**全量同步到 6 份项目文档。按用户要求：**未改《原型线框图.html》与《导航交互原型.html》**，仅改文档。

## 新配色（用户给定 RGB → hex，4 权威令牌：主背景 / 侧栏 / 卡片 / 前景）
| 主题 | 主背景 `--background` | 侧栏 `--sidebar` | 卡片 `--card` | 前景 `--foreground` |
|---|---|---|---|---|
| `light` 浅色 | `#F5F5F5` | `#FFFFFF` | `#FFFFFF` | `#424242` |
| `dark` 深色 | `#121212` | `#202124` | `#202124` | `#E0E0E0` |
| `beige` 暖色 | `#FFF3E0` | `#FFF9EE` | `#FFF9EE` | `#424242` |
| `green` 环保 | `#AED59C` | `#F1F8E9` | `#F1F8E9` | `#424242` |

派生令牌（按参考图交互态推导）：`--selected` / `--border` = light `#E5E5E7` · dark `#3C4043` · beige `#F0E3CB` · green `#DCE8CE`。**旧 16 值全部作废。**

## 参考图 UI 要点（已写入规范）
- 侧边栏 = 独立悬浮卡片（仅左圆角 16px + 向右投影 `4px 0 20px`）
- 右内容区背景 = 主题「主背景」色（不再两栏纯白无缝）
- 导航项 = 24px 图标 + 17px 文字、间距 28px、hover 变暗上浮 / active 更深下沉
- 卡片 = **无描边（border none）** + 圆角 12px + 投影 `0 6px 24px`，宽 90% 右缩，hover 上浮变暗 / active 下沉
- 右上角 = 窗口控制三按钮（放到后台/全屏/关闭，24×24，`#666` → Hover 变深，距顶/右 12px）——**替换原 CloseX 44×44 距顶28/右60 约定**
- 导航/卡片文字用项目实际（简历/AI/设置 + 新建空白简历/打开或最近/导入/管理多份简历/岗位目录）

## 已同步文档（6 份）
1. **《技术栈.md》** §3.15/§3.15.1：规范表全行重写 + 5 令牌表 + 落地要点 + 窗口控制按钮 + 防绿字护栏（6 令牌）
2. **《项目功能.md》** F18 需求/护栏/落地形态令牌表 + 四章主壳与视觉形态 + §五 操作路径（主题显示名：浅色/深色/暖色/环保）
3. **《项目介绍.md》** F18 行 + 文档地图（新增参考图条目、主题四色方案标注待重做）
4. **《项目规范.md》** 二·配色职责边界（6 令牌 + 防绿字护栏断言更新）
5. **《项目实现情况.md》** §一 追加五次定案条 + F18 行状态更新
6. **《项目日志.md》** 已登记本次变更

## 边界与待办
- 未动线框图/导航交互图（用户明确暂缓）——二者仍为四次定案基线，与文档临时不同步，待用户拍板后按三步铁律走 ②更新线框 ③重跑生成器
- `file/rubbish/UI示例_主题四色方案.html` 旧配色值已过期，**待按新令牌表重做归档**（用户本次要求只改文档，未更新）
- 主题 id 不变（light/dark/beige/green），仅显示名与配色更新；`--ui-font`/`--resume-font` 双域正交、PreviewPane 白纸、F4 三套模板均未动
