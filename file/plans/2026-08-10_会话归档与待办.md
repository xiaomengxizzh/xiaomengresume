# 2026-08-10 会话归档与待办

> **定位**：2026-08-10 多轮会话（v4 架构收敛批 / 用户质疑实证修复批 / 导入通用化改造批）全量记录 + 收尾待办 + commit 清单。配套：完整流水见《项目日志.md》2026-08-10 各条目。

## 一、本会话完成批次（按时间序）

| 批次 | 内容 | 验证 |
|---|---|---|
| **导出 PDF 差异修复批** | 字体等线 Deng/Dengb 真加粗 + 逐区字体 + 中西文混排 / P0-2 双间距 / P0-3 字号二次换算 / P0-4 灰线 / P1 全项（compact 字号·头像缺省·链接色·隐私头像·modern padding·空 summary·avatar 资源·长值截断）/ 视觉核验闭环 a-e 一致 | 235/235 |
| **排版一致性架构收敛批 v4** | 单一事实源 `shared/templates/layout.ts`（TEMPLATE_PRESETS/lv/fmtDate/TYPE_SCALE/titleStyleLogic/sectionSpacingLogic/entrySpacingLogic/CONTACT_GRID_LOGIC/LIST_MARK_LOGIC/INFO_ICON_ELEMENTS/compactSpacing）+ 两端改引用零拷贝 + 结构守卫测试 6 例 + F1-F9 公式对齐 + 任务 1 图标对齐 + 任务 3 A4 辅助线 + 任务 4 紧凑排版 + 任务 5 IconPicker + 导入标签全量 + flush 加固 | 243/243 |
| **用户质疑实证修复批** | 标签字号 bug（emPx 误用 +33%→×0.75）/ 标签位置（44%→48% 两列）/ B 档同行拆分 + 裸职业 fallback（material 7 字段）/ 分页外部实证（官方文档 + 4.7.0 源码：lineHeight 无最小限制、断点=shouldBreak 防孤立策略、wrap/break/minPresenceAhead 官方机制；已加条目 wrap={false} + pageCount 透传，断点本身如实登记引擎特性） | 243/243 |
| **导入通用化改造批** | B 档坐标级候选对 extractPdfLines（unpdf 坐标→y 聚类行 + 两列 pairs，文本沿用 extractText 阅读序）+ rules 三来源候选（冒号/空格 1+/坐标 pairs）全进 customFields + knownLabels 收窄 + A 档 prompt 任意标签约束 + ZodError 重试 + 导出 buildResult 作用域修复 | 246/246 |

## 二、待完成事项（按优先级）

**P0 真机/环境**
1. ✅ **git commit 已执行（2026-08-10 收尾）**——git 位于 `C:\Users\zzh\scoop\apps\git\current\cmd\git.exe`（scoop，此前 where /r 漏搜）；6 批提交完成：887422a(feat settings) / 949a40a(feat import) / f9f3cfe(fix export+refactor 收敛) / 60120bf(feat+chore 岗位/批量/四宫格/清理) / 50b35d8(docs) / ebcfb39(feat shared 补遗)。**push 未执行**（外向操作需用户确认；push 前先 `git fetch` 检查远端外部改动）。工作区剩余未提交 = 非项目产物（material 核验物/用户材料、.workbuddy、ui_screenshots*、rubbish 过程文件）。
2. **用户实测导入仍"没有解决"**——material 实证 7 字段全识别，但用户实际文件场景未复现定位；需用户提供失败的具体 PDF/Word 文件路径 → 对照 B 档（坐标候选）与 A 档（LLM）实际输出诊断。可能方向：无冒号且紧贴式标签、扫描件（needsVision 走占位）、A 档模型输出不完整、旧 build 未重启。
3. **分页断点**（p1 半页空白）——@react-pdf 引擎防孤立策略，断点不可强制填充；已加条目 wrap={false} + 真实 pageCount 透传。可选下一步：评估换 Puppeteer/Chromium printToPDF（与预览同引擎，但违背"纯代码零 GPU"定案，需用户拍板）。

**P1 功能**
4. M4b vision（扫描件/图片导入，PP-UIE 0.5B Python sidecar 候选——需拍板是否接受 Python 运行时打破纯 Node）。
5. 英文模式（basics.englishName 预留，UI 隐藏）。
6. 照片位置可调/隐藏（M5 模板编辑）。

**P2 清理**
7. `material/核验导出_修复后.{pdf,png×2}`、`核验预览_修复后.{html,png}` 核验产物（建议保留供查看）。
8. `ResumesJobs.tsx`/`JobsHome.tsx` 等死代码——已在前次 P2 批处理，本轮无新增。

## 三、分批 commit 清单（Git Bash 环境执行）

```bash
cd /e/ai/aiwork/newproject/xiaomengresume
# 1. 导出 PDF 差异修复批
git add -A -- project/src/main/export project/src/renderer/src/templates project/src/renderer/src/styles.css project/src/renderer/src/i18n project/src/shared/constants/fonts.ts project/resources/avatar.png
git commit -m "fix(export): 导出PDF与预览差异修复(等线字体+真加粗/双间距/字号换算/灰线/P1全项)"
# 2. 架构收敛批 v4
git add -A -- project/src/shared/templates project/src/renderer/src/templates project/src/main/export project/src/renderer/src/components/editor/IconPicker.tsx project/src/renderer/src/components/icons/InfoIcons.tsx project/src/renderer/src/components/editor/LayoutBar.tsx project/src/shared/__tests__/layout-consistency.test.ts
git commit -m "refactor(export): 排版单一事实源shared/templates/layout.ts+两端引用零拷贝+结构守卫+F1-F9对齐+紧凑排版+IconPicker"
# 3. AI 设置程式化批 + R1-R10（若此前未提交，合并）
git add -A -- project/src/renderer/src/views/SettingsAi.tsx project/src/renderer/src/components/ai project/src/renderer/src/i18n project/src/shared/ipc-channels.ts project/src/main/ai
git commit -m "feat(settings): AI服务商全开放+温度三档+提示词卡重构+config:reset"
# 4. 导入标签全量 + 导入通用化
git add -A -- project/src/main/import project/src/shared/schema/import-map.ts project/src/shared/__tests__/import-map.test.ts
git commit -m "feat(import): 坐标候选对+三来源customFields+任意标签约束+补写字段"
# 5. 文档同步
git add -A -- file/
git commit -m "docs: 2026-08-10 会话文档四连同步"
# 6. 岗位状态/批量导入/其余批次（若未提交）
git add -A -- project/src/shared/schema/job.ts project/src/main/files/job-store.ts project/src/renderer/src/views/JobsManager.tsx project/src/main/import/run.ts project/src/preload/index.ts project/src/renderer/src/views/ResumesManager.tsx
git commit -m "feat: 岗位未投状态+批量导入runBatch"
# 最后：git fetch + 检查远端外部改动后 push（开源仓库）
```

## 四、关键坑位速查（续接必读）

- **pdf.js y 坐标从页面底部向上增长**——extractTextItems 聚类行必须降序，否则行序倒置（name/headline 全乱）。
- **infoFontSizePt 用 emPx 换算 = px 值当 pt（+33%）**——px→pt 必须 ×0.75。
- **buildResult 作用域**：try 块内 const 不能在 try 外返回处引用。
- **"实习天数 3"被 headline fallback 抢**——裸职业 fallback 限"纯中文无空格数字"。
- **knownLabels 只含固定映射集**——籍贯/年龄等自定义标签必须保留入库。
- **分页断点** = shouldBreak 防孤立策略（minPresenceAhead），官方机制 wrap/break/minPresenceAhead，非行距限制。
- **临时脚本已清**（tmp_*/.tmp_ssr_*/ui_diag_*/tmp_pdf2png.py 全部删除；ui_capture*/docs-tool/git_docs_commit/split_docs 保留）。

## 五、环境备忘

- 本环境 Bash = system shell（git/python 不可用、node=E:\tool\nodejs\node.exe、vitest 需 set PATH 前缀、cmd 引号破坏 node -e 内联脚本→用 .cjs 文件）。
- Electron 不可启动（无 GPU）→ 核验用 SSR（esbuild bundle + Edge headless 截图）+ pymupdf 渲染 + doubao 视觉。
- docs-tool.py 经 C:\Windows\py.exe 运行。
