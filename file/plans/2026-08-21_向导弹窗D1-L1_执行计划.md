# 向导弹窗（D1-L1）执行计划（2026-08-21）

> **定位**：讨论稿 `2026-08-21_未来发展增量讨论.md` D1 二轮细化经用户拍板（「暂时设计成这样」）后的落地执行计划。
> **上级文档**：《项目功能.md》（落码时登记 **F22 使用向导**，编号顺延现行最大 F21）· 《项目介绍.md》§二界面形态 · 讨论稿 T4。
> **版本闭环**：纯渲染层；**零 IPC 契约变更 · 零新依赖 · 零 AI · 不动 store/schema**；唯一正式文档变更 = 三文档同步批（B5）。
> **执行纪律**：TDD 先红后绿；每批过 typecheck+lint；B3 后用户抽查确认再进收口批（M5 执行规则）。

## 〇、范围与定案

| 定案项 | 内容 |
|---|---|
| 入口 | 主：NavBar 底部 nav-spacer 上方常驻「使用向导」（collapsed 折叠态不放）；辅：WelcomeView 欢迎卡第三按钮（ghost） |
| 形态 | 点击开向导弹窗（复用 `ui/dialog.tsx`）；弹窗内「选择问题 → 分步指引」，每步可带「前往 →」直调 `setCurrentView` 并关闭弹窗 |
| 问题集 v1 | 7 条：新建简历 / 导入旧简历 / 换模板配色 / AI 功能说明（含 BYOK）/ 导出 PDF / 多份简历管理 / 数据存哪 |
| 弹窗状态 | 各入口本地 useState 各挂一个 `<WizardDialog>`（未 open 渲染 null，幂等零侵入）；重开回问题列表顶层 |
| 明确不做 | L2 AI 问 AI 入口（仅留注释锚点）/ Ctrl+K 命令面板 / 首启 onboarding 清单 / 弹窗内截图插图 / 焦点陷阱增强 |

## 一、依赖图

```
B1 数据与类型 ──► B2 WizardDialog 组件（TDD）──► B3 双入口接线 + i18n ──► 【用户抽查】
                                                                        └─► B4 文案打磨 + 截图复核 ──► B5 收口（三文档同步 + 四连）
```

## 二、批次详情

### B1 · 数据与类型（渲染层常量，先行）
- 新建 `project/src/renderer/src/components/wizard/wizard-topics.ts`：
  - `type WizardTargetView`：字面量联合，值域对齐 `App.tsx:131-152` 分支（welcome/editor/resumes-home/resumes-list/resumes-recent/resumes-new/resumes-manage/jobs-manage/import-home/ai-home/ai:grammar/ai:intro/ai:polish/ai:match/settings-home/settings-appearance/settings-templates/settings-ai/settings-storage），注释注明「与 App.tsx 分支同步」；
  - `interface WizardStep { textKey: string; targetView?: WizardTargetView }`；
  - `interface WizardTopic { id: string; categoryKey: string; titleKey: string; hintKey: string; steps: WizardStep[] }`（hintKey = 完成标志行）；
  - `WIZARD_TOPICS: WizardTopic[]` 7 条 + 分类键 4 个（gettingStarted/import/ai/exportSettings）。
- 初始 7 题目标视图：新建→resumes-new；导入→resumes-new（T8：新建二级选择含导入）；模板配色→settings-templates；AI 说明→settings-ai；导出 PDF→editor；简历管理→resumes-manage；数据存储→settings-storage。
- **验收**：typecheck 过；测试 `wizard-topics.test.ts` 红→绿（id 唯一 / steps 非空 / targetView ∈ 联合 / hintKey 非空）。

### B2 · WizardDialog 组件（TDD）
- 新建 `project/src/renderer/src/components/wizard/WizardDialog.tsx`：
  - props `{ open, onClose }`；内部 `useState<'list' | stepIndex 视图>` 两态：问题列表页（按分类分组小标题 + 问题按钮）→ 步骤面板页（编号步骤 + 「前往 →」[有 targetView 时] + 底部完成标志 hint 行 + 返回按钮）；
  - 「前往」= `setCurrentView(targetView)` + `onClose()`；
  - 关闭重置回 list 态（Dialog unmount 天然满足——open=false 返回 null）；
  - 弹窗底部预留注释锚点：L2「没找到？问 AI」入口位；
  - 样式走令牌（border-border/bg-surface/text-foreground），禁硬编码色，禁硬编码中文（全部 i18n key）。
- 测试 `__tests__/wizard-dialog.test.tsx`（RTL；**afterEach(cleanup)** 手动注册——vitest 无 globals 既有教训）：open=false 不渲染 / 渲染 7 题标题 / 点题进步骤面板 / 「前往」触发 setCurrentView+onClose / 返回回列表。
- **验收**：新测试红→绿；typecheck+lint 过。

### B3 · 双入口接线 + i18n
- `NavBar.tsx`：底部 `<div className="nav-spacer" />` 上方插入向导按钮（24px 线框 SVG 图标风格对齐 `MainIcon` + `t('nav.wizard')`，样式类对齐 nav-sub/nav-main 令牌）；collapsed 分支不加；本地 state 挂 `<WizardDialog>`。
- `WelcomeView.tsx`：欢迎卡按钮区第三枚 `<Button variant="ghost">`（`t('welcome.wizard')`）+ 挂 `<WizardDialog>`。
- i18n `zh/en` 同步新增：骨架（wizard.title/wizard.back/wizard.goTo/wizard.doneHintPrefix 等）+ 分类 4 + 7 题 ×（title+hint+2~4 步）≈ **50 键双端对称**；跑 `check:i18n` 断言 lost:0。
- **验收**：typecheck/lint/vitest 全绿；`check:i18n` 通过；playwright mock 应用壳截图（NavBar 底部观感 / 弹窗列表态 / 步骤面板态）自查。
- ⏸ **治理节点：截图交用户抽查确认，通过前禁进 B4/B5。**

### B4 · 文案打磨 + 截图复核
- 7 题步骤文案人话化终审（依据《项目功能》对应 F 条目提炼；每步一句 + 可直达；完成标志可判定，如「看到『导出成功』提示即完成」）；
- 英文文案母语级校对（非机翻直译）；
- 复跑 check:i18n + 截图复核长文案不撑破弹窗（Dialog max-h-[85vh] overflow 兜底）。
- **验收**：check:i18n 通过；截图复核三态正常。

### B5 · 收口
- 全量验证：typecheck 双端 / lint --max-warnings 0 / vitest（基线 295/295 起，新增测试计入）/ build 三端 / check:i18n。
- **三文档同步铁律批**（同批 commit）：
  - 《项目功能.md》：新增 **F22 使用向导**（入口/形态/问题集/i18n）+ 开头索引表；
  - 《项目介绍.md》§二界面形态句尾追加「使用向导弹窗」一句；
  - 《技术栈.md》：**无需改动**（纯 React 组件零新技术）——G.7 自检 B 显式标注此豁免及理由；
  - 《项目实现情况.md》§一「已实现」补 F22 条目；《项目日志》当日条目（月度明细 + 主索引）。
- `scripts/docs-tool.py selfcheck` 全绿；规范 message commit（禁 --no-verify）。

## 三、用户旅程验收清单（专节——验收看「能否走通」）

- **V1 冷启动求助**：首启欢迎页 → 点「使用向导」→ 弹窗列出 7 题 → 点「如何新建简历」→ 步骤面板出现 → 点「前往」→ 直达新建流程且弹窗关闭。
- **V2 编辑中迷路**：编辑器内 → NavBar 底部向导按钮 → 选「怎么导出 PDF」→ 步骤指引清晰可照做。
- **V3 语言切换**：切 English → 向导全部文案为英文、无 key 串裸露、布局不破。
- **V4 折叠侧栏**：侧栏折叠后无向导按钮；展开后恢复。
- **V5 关闭行为**：Esc 与点遮罩均可关闭；再次打开回到问题列表顶层。
- **V6 工程闸门**：check:i18n zh/en 对称 lost:0；vitest 全绿（含新增用例）。

## 四、风险与预案

| 风险 | 预案 |
|---|---|
| targetView 拼写漂移（字符串路由） | 字面量联合类型编译期拦截 + B1 测试断言值域，注释锚定 App.tsx 行号 |
| 步骤长文案撑破弹窗 | Dialog 既有 max-h-[85vh] + overflow-y-auto；B4 截图复核 |
| RTL teardown 竞态噪音 | afterEach(cleanup) 手动注册（项目既有教训两次命中） |
| NavBar 底部拥挤 | spacer 弹性吸收；按钮单行 32px 级；B3 截图确认 |

## 五、边界与后续

- 本批仅 D1-L1；L2（AI 问答直达）、Ctrl+K 命令面板、首启 onboarding 清单仍在讨论稿待办，不混入本批。
- 讨论稿 T1/T2/T3（铁律修订 / vision 导入 / DeepSeek 服务商）不受影响，另行拍板。
