# 2026-08-13 会话归档（遗留复查 → 模板图书式重设计 → 导入测试集 → 简历2修复 → 四项需求批）

> **定位**：长期存档（复盘基准）。本次会话 = 遗留问题复查 + 模板编辑界面图书式重设计（含布局方案 A）+ 导入 B 档测试集与评测 + 简历2 映射修复（两轮）+ 四项需求批 A-D + 问题修复批。
> **上级**：`file/plans/2026-08-12_M5里程碑_会话归档.md`（M5 收口）→ 本归档（M6 前界面调整批 + 导入验证闭环 + 四项需求）。真相源仍在 6 主文档 + detail/logs。

## 一、commit 链（main）

| commit | 批次 | 内容 |
|---|---|---|
| `e736b4a` | 遗留复查 | W1-W3/D1/日志入口复核通过 + 四色 html 经用户确认删除 + 三文档引用同步 |
| `778370d` | 模板重设计① | 模板设置屏图书式翻书（TemplateBook 中突出两侧露边 + 箭头循环 + 独立编辑视图 + i18n 补 19 键修复 key 串 bug） |
| `5613377` | 模板重设计② | 布局方案 A：卡片随容器自适应缩放（ResizeObserver + 1:1.8:1 分配 + 侧卡全显 + 最小宽 143px 保护） |
| `0ed7bbd` | 导入测试集 | 50 份 PDF（S1 自造4/S2 开源21/S3 真实25）+ expected.json ground truth + import_bench.mjs（天池三级 F1）；结果 0.71/0.38/0.36 |
| `b6a5f58` | 简历2修复① | 校园经历/相关技能锚点补齐 + basics 同行多字段提取 + 无后缀城市 location；简历2 1.00 |
| `311b36c` | 简历2修复② | basics 同行多 label 拆分（matchAll + 负向前瞻值截断，性别/年龄拆两条） |
| `82435cb` | 简历2修复③ | 系统性五修：日期切分收紧（1535 不误切）/ 锚点行内容保留（languages 恢复）/ education degree-major / employmentStatus 状态:值直取 / location 兜底 / TITLE_HINTS 扩 |
| `b7ea9e1` | 四项需求 A-D | gender/age 正式字段 + 自动一页纸等比重排（fitToPage+0.6下限）+ 导入模块显隐+自定义模块多模板（text/entry/tag）+ 条目项目符号（listMark）；全仅增不改 |
| `409eb13` | 问题修复 | 新建模块无反应（window.prompt Electron 未实现→Dialog）+ 岗位保存错误静默（JobsManager save 加 catch+alert） |

## 二、批次摘要

- **遗留复查**：M5 收口遗留 6 项，5 项复核通过（W1 i18n/W2 白名单/W3 snapshot/D1 注记/日志入口），四色 html 经用户确认删除 + 三文档引用同步。
- **模板图书式重设计**：用户给参考图（蓝底+3 橙块，doubao 视觉解析）+ 口述定案——翻书感中间突出两侧露边、箭头循环（未来多模板翻页）、卡片零按钮点击进编辑、独立编辑视图带返回、配色用当前主题。方案 A：ResizeObserver 量容器宽 → 1:1.8:1 分配 → scale=槽位宽/794（侧卡全显+gap 28+最小宽 143）。
- **导入 B 档测试集**：50 份 PDF + expected.json（S1 数据源精确/S2S3 人工核对）+ import_bench.mjs（天池三级 F1，Node24 type stripping 直 import rules.ts）。结果 S1 0.708/S2 0.376/S3 0.355——真实世界 PDF（LinkedIn）basics 仅 0.071（Page 标记污染 + 无联系方式生效条件）。
- **简历2 映射修复（用户真实简历）**：三轮——①锚点（校园经历/相关技能）+同行多字段；②matchAll 拆对；③系统性五修（日期切分/锚点行内容保留/degree-major/employmentStatus/location 兜底/TITLE_HINTS）。简历2 达 1.00，S1 → 0.817。
- **四项需求批 A-D**：②gender/age 正式字段 / ④自动一页纸（等比重排 fitToPage+0.6 下限）/ ①导入模块显隐+自定义多模板（text/entry/tag）/ ③条目项目符号（listMark）。全仅增不改零迁移，预览/导出同源一致。
- **问题修复**：新建模块 window.prompt→Dialog（Electron 渲染进程无 prompt）；岗位保存错误静默加 catch+alert（根因待真机）。

## 三、关键决策

1. 模板图书式：配色用当前主题（不用参考图蓝/橙）；翻书中间突出；箭头循环；卡片零按钮点击进编辑；编辑独立视图带返回；方案 A 自适应缩放（守容器复用铁律，不放宽全局容器）
2. 导入测试集：S2 只留中英；S3 用 LinkedIn parser+bjherger（本地匿名化不入库）；弃用爬虫渠道（形态不匹配+合规红线）；评测天池三级加权 F1；A 档留样本后续 XM_AI_KEY 对比
3. 简历2 修复原则：一切不一致都算（用户定）；裸 4 位数字不误判日期；锚点行内容不丢弃；「实习」词表用「实习中」防误伤「实习天数」
4. 四项需求：模块按数据显示仅导入路径生效（新建空白默认全模块）；自定义模块选固定模板（text/entry/tag 三套）；自动一页纸等比重排+0.6 下限退回多页；项目符号=条目正文列表而非标题

## 四、坑位（已解决/登记）

- **window.prompt 在 Electron 渲染进程未实现**（恒 null）→ 新建模块改用 Dialog（window.confirm/alert 正常）
- **Mimosa 误报**：正则 `mRe.exec()` 被误判命令注入（正则 exec 非 shell exec）→ 改 matchAll 写法规避；Bash 写源码被拦 → 用 Write/Edit
- **Edge headless 沙箱拒写 PDF**（print-to-pdf 拒绝访问）→ S1 改 reportlab（Python 4.4.10）
- **沙箱 curl/api.github.com 被拦（000）** → git clone 通道可用，S2/S3 全部走浅克隆
- **unpdf 经 createRequire 从 project 解析**（scripts/ 目录 node_modules 解析不到）；Windows 路径需 pathToFileURL
- **Node type stripping import .ts 需显式 .ts 扩展名**（import-map.ts 引 resume 无扩展名报错）
- 启发式计数不可靠（正则数 education/work 条数误判）→ expected 人工核对

## 五、验证状态

- **沙箱**：typecheck / lint --max-warnings 0 / vitest **280/280** 全绿（会话末）；import_bench 51 份（S1 0.817 / S2 0.377 / S3 0.331，简历2 1.00）；selfcheck 全绿 diff 0
- **真机待验**：模板图书式翻书观感/自适应缩放；fitToPage 压缩观感/下限提示；导入模块过滤+添加；entry/tag 模板渲染；项目符号双端一致；岗位保存错误提示

## 六、遗留 / 待办

- ⏳ **自动一页纸间距问题**：模块最后一行与下一模块标题间距太大（用户反馈——sectionGap 缩放后仍偏大？或 fitToPage 时 sectionGap 未等比？待查）
- ⏳ **项目符号位置修正**：用户澄清符号应放**模块内正文每一句简历开头**（非模块标题/条目头）——当前实现是条目列表头（education/work/projects entryHead），需改为正文段落/列表项级
- ⏳ **岗位保存根因**：错误静默已修（catch+alert），用户重跑看具体错误后可定位
- ⏳ **S3 LinkedIn basics 0.071**：Page 标记污染修复（报告 P0 第 1 条：strip 分页标记）
- ⏳ **M6 前置**：vision 导入闭环 / 默认头像拍板 / 图片版导出（capturePage）/ 英文版 summary 联动 / JSON Resume 映射 / 火山 baseURL 核对
- ⏳ **M7 打包发布**：F14 自动更新 + F15 electron-builder 三端 + Release
- ⏳ 界面调整批剩余：英文 UI 适配（模板重设计已做，英文适配待走查）
