# 2026-08-12 M5 里程碑 · 会话归档

> **定位**：长期存档（复盘基准）。本次会话 = M5 应用功能收尾全批（M5-1~M5-7 + 2 补丁）+ 里程碑重排（M5/M6/M7）+ 界面调整决策。
> **上级**：`file/plans/M5-M7_里程碑重排_2026-08-12.md`（框架）→ 本归档（执行复盘）。真相源仍在 6 主文档 + detail/logs。

## 一、commit 链（main）

| commit | 批次 | 内容 |
|---|---|---|
| `b3e957b` | 前置 | 里程碑重排定案（M5 功能收尾/M6 增强/M7 打包）+ plans + 六文档同步 |
| `5291155` | M5-1 | 定位同步（防盲信去卖点）+ 示例数据全局替换（王晨/logo 头像）+ 契约冻结（templates/defaultTemplateId/font:import/logs:export） |
| `6ded5d7` | 修 | 简历名称输入框回归测试（报障处置：GPU 合成层已知问题需重启 dev） |
| `32de533` | M5-2 | 窗口批：无边框 + 三按钮 + 托盘（方案 A）+ 窗口状态记忆自写 + D7 关窗保存（window:before-hide） |
| `e4a3a52` | 修 | 导入简历标题无法编辑（根因：顶层无 title 字段 → immutableSetByPath 抛错） |
| `f9e43f8` | M5-3 | 模板参数化：settings:get/set 链路 + A3 三层覆盖链 + A7 字体分离 + A4 编辑组件 |
| `d7d4374` | M5-4 | 设置屏+字体：外观屏 + 自定义主题（derive 接线）+ 存储屏 + font:// 字体系统 |
| `47d2598` | 补丁 | 语言切换接线修复（changeLanguage）+ 关闭行为选项（tray/quit 首次弹窗） |
| `8780f47` | M5-5 | 模板设置主功能：第 4 子项入口 + 预览屏 + 新建选模板 + 默认模板 + 示例共享化 |
| `2537dcf` | M5-6 | 导入向导美化（C1-C4）+ A8 导入选模板 + electron-log 日志 + D8 关于页 + D9 错误 i18n |
| `7b5d823` | M5-7 | 删关于页 + 模板卡真实渲染（TemplatePreviewCard）+ axe landmark |

## 二、M5 批次摘要

- **M5-1 地基**：定位同步（"防盲信 AI"去卖点 → 简单好用）/ 王晨示例全局替换 / 契约冻结（SettingsSchema.templates+defaultTemplateId / Font / Logs）
- **M5-2 窗口**：frame:false + 自绘三按钮 + 托盘（关窗驻留，菜单打开/退出，品牌 logo）+ 窗口状态记忆（自写 window-state.json）+ 关窗保存（window:before-hide 事件链）
- **M5-3 模板参数化**：渲染层 settings 链路打通（settings:get/set）+ 三层覆盖链（per-resume > 模板覆盖 > 出厂）+ 字体分离 + TemplateSettingsEditor
- **M5-4 设置屏+字体**：外观屏（4 色/跟随系统/语言/300ms）+ 自定义主题（deriveTokens 派生）+ 存储屏 + font:// 协议 + queryLocalFonts + 导入管理
- **M5-5 模板设置主功能**：第 4 子项入口 + 预览屏（示例真实渲染）+ 新建选模板 + 默认模板 + 示例共享化（shared/sample-resume.json）
- **M5-6 向导+收尾**：C1 并排对照 / C2 AI 来源提示 / C3 步骤指示 / C4 EmptyState / A8 导入选模板 / electron-log（5MB×3 轮转）/ logs:export / 导出错误 i18n
- **M5-7 收尾**：删关于页（用户定案，日志入口移存储屏）/ 模板卡真实渲染（TemplatePreviewCard）/ axe landmark

## 三、关键决策

1. 里程碑重排：功能先行、打包最后（M5 收尾 / M6 增强 / M7 打包）
2. 托盘方案 A：关窗驻留（before-quit 置 isQuitting 放行真退出）；closeBehavior 可设 + 首次弹窗
3. 模板覆盖链三层 + 出厂配方维护 ui-config（M5-3 扩展生成器，待 M5 后补）
4. 字体 family v1 用文件名（零依赖）；queryLocalFonts 枚举（Joplin 路线）
5. 自定义主题 = customTheme.primary → deriveTokens 内联覆盖 6 变量
6. 界面调整（用户）放遗留后、M6 前：英文 UI 适配 + 模板编辑界面设计（不满意项）

## 四、坑位（已解决）

- 示例 schema 中文枚举（skill level/language proficiency）/ meta.lastOpenedAt 不可 null
- TemplateOverrideSchema 用前声明（TS2448）/ immutableSetByPath 顶层无 title 字段
- electron-log archiveLogFn 参数是 LogFile（file.path）
- Electron Permission 枚举无 local-fonts（字符串断言）/ mkdir 返回类型
- Mimosa 钩子多次误拦 Bash 写源码路径（改用 Edit/Python）

## 五、验证状态

- **沙箱**：typecheck / lint --max-warnings 0 / vitest **273/273** 全绿；selfcheck CH1/CH5/CH7 绿
- **真机**：用户已跑 E1–E4（窗口/smoke/material/设置屏）验证通过（2026-08-12）

## 六、遗留 / 待办（M5 后、M6 前）

- ⏳ **界面设计调整批（用户驱动，M6 前完成）**：英文 UI 适配 + 模板编辑界面设计 + 其他界面调整（清单待用户列）
- ⏳ 出厂配方入 ui-config（生成器产 CSS+配方 JS）——M5-3 已预留未做（M5 后补）
- ⏳ 日志导出入口：已移存储屏「导出日志」按钮 ✅（本批处理）
- ⏳ 四色演示 html（rubbish/UI示例_主题四色方案.html）：旧值过时，标注可清理（令牌权威在技术栈 §3.15.1）
- 📌 已登记不处理：SettingsAi 标题 y17px（结构差异）/ photo 快照引用共享（已随转存降低）/ lastEditedPath 闪烁（已拆分）/ axe landmark（已修复）

## 七、下一步

- 界面设计调整批（M6 前）→ M6 功能增强批（vision 导入/图片版导出/英文版联动/JSON 映射）→ M7 打包发布（electron-builder + Release + F14 自动更新）
