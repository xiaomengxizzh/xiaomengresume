# M5 应用功能收尾 · 细颗粒执行计划（2026-08-12 落盘）

> **定位**：M5 里程碑执行计划（长期存档，复盘验收基准）。范围 = 用户已冻结 A–F（不许增删，只许微调顺序）。
> **状态**：✅ 范围已冻结；3 个假设点 + 批次顺序**待用户确认**（见 §五），确认后开工。
> **上级文档**：`file/plans/M5-M7_里程碑重排_2026-08-12.md`（里程碑框架）→ 本计划（细颗粒）。

## 〇、范围冻结（A–F）

- **A 模板设置主功能**：A1 设置第 4 子项入口 / A2 模板预览屏（示例数据，编辑保存后才更新）/ A3 全局模板参数可编辑（覆盖链 per-resume > 模板配置 > 代码默认，可还原，导出不受影响）/ A4 模板编辑界面 / A5 新建空白先选模板 / A6 默认模板可设 / A7 字体分离（模板默认字体 vs 本简历覆盖）/ A8 导入确认写入步插模板选择
- **B 新示例数据**：B1 自创示例（不与他软件雷同，头像=品牌 logo，内嵌不进简历目录，替换「打开示例」数据源）
- **C 导入向导微调（保留三步核对）**：C1 ①步并排对照 / C2 ②步可编辑提示 + AI 来源标记 / C3 步骤指示器对齐新版 UI / C4 EmptyState 统一
- **D 原 M5 九项**：D1 F18 外观屏（4 色+跟随系统+语言+300ms 实时预览）/ D2 自定义主题接线 / D3 F21 存储屏 + reset 二次确认 / D4 窗口批（无边框+三按钮+托盘[关窗驻留，菜单打开/退出，品牌 logo]+窗口记忆自写+Win10/11）/ D5 字体系统（font:// + queryLocalFonts + 许可提示）/ D6 本地日志（electron-log + archiveLogFn 5MB×3 + 导出日志）/ D7 关窗保存兜底（save-now 时序）/ D8 关于页（版本+开源链接+导出日志；许可/贡献走项目文件）/ D9 导出错误 i18n / D10 微调卫生（axe landmark / y17px / photo 快照 / lastEditedPath 闪烁 / 四色演示 html）
- **E 真机验证批**：E1 窗口批 / E2 smoke 双轨 / E3 material 对照+O2+2 列+photo+居中+重导出 / E4 设置屏走查
- **F 文档与契约**：F1 定位同步（防盲信残留清理，口径=用户批准推荐项）/ F2 契约冻结（templates 覆盖层 + defaultTemplateId + font:import/font:remove/logs:export）/ F3 依赖登记

## 一、依赖图（简版）

```
M5-1 地基批
  F1 定位同步（纯文档，独立）        ┐
  B1 新示例数据（独立，A2/A5 数据源） ├─ 都不依赖别人，最先做
  F2 契约冻结（Schema/IPC 变更一次定）┘
        │
        ├─→ M5-3 模板参数化：A3 → A4 → A7   （A3 依赖 F2 templates 字段）
        ├─→ M5-4 设置屏+字体：D1 → D2 → D5  （D5 依赖 F2 font IPC + D1 入口；D3 独立）
        ├─→ M5-5 模板设置屏：A1/A2（依赖 B1）/A5（依赖 A2 组件）/A6（依赖 F2 defaultTemplateId）
        └─→ M5-6 向导+收尾：C1–C4 + A8（插 C 批改的向导）+ D6（依赖 F2 logs 通道）→ D8（依赖 D6）
M5-2 窗口批：D4 + D7（独立）→ 真机 E1 可紧随（用户要求窗口批真机验证最前）
M5-7 总验证：E2/E3/E4 + D10 —— 依赖前面全部
```

## 二、批次总览

| 批 | 名称 | 覆盖 | 依赖 |
|---|---|---|---|
| M5-1 | 地基（定位+示例+契约） | F1, B1, F2 | 无 |
| M5-2 | 窗口批 | D4, D7 | 无（独立） |
| M5-3 | 模板参数化 | A3, A4, A7 | F2 |
| M5-4 | 设置屏+字体 | D1, D2, D3, D5 | F2 |
| M5-5 | 模板设置主功能 | A1, A2, A5, A6 | B1, F2 |
| M5-6 | 导入向导+收尾 | C1–C4, A8, D6, D8, D9 | F2 |
| M5-7 | 真机总验证 | E2, E3, E4, D10 | 全部 |

## 三、各批详述

### M5-1 地基批 —— 先把"图纸和模特"定下来
- **目的（人话）**：三个互不依赖的地基——文档过时定位改掉、造"模特简历"当模板预览素材、新接口（模板参数/字体/日志）一次画好红线，后面每批照接口做不返工。
- **覆盖**：F1 + B1 + F2
- **产出**：①文档（介绍定位语/三板斧②/承诺修订 + 技术栈 543 口径 + 日志项目定位节）②新示例简历（自创人物，头像=品牌 logo，替换 sample-resume，内嵌不落盘）③契约（settings.ts 增 templates 覆盖层 + defaultTemplateId；ipc-channels 增 font:import/font:remove/logs:export；重跑 gen_api_docs.mjs）
- **验收**：
  1. `python scripts/docs-tool.py selfcheck` → CH5 已登记 ✅、CH1 无断裂
  2. `grep -rn "防盲信" file/项目介绍.md` → 仅功能事实层（或按口径清零）
  3. `cd project && npm run typecheck` → 无错误
  4. `cd project && npx vitest run` → 254/254 + 新契约测试全绿
  5. 打开应用 → 「打开示例」→ 新人物示例（非宋哈娜）
- **风险**：契约字段遗漏（预警：gen_api_docs 与代码不一致即停）；示例 schema 不合法（预警：migrate 抛错，先 migrate(sample) 单测兜住）
- **边界（不做）**：任何功能 UI、不建模板设置屏
- **治理**：⚠️ F2 需审批（SettingsSchema + IPC 契约，集成者批准）；三文档同步（介绍/技术栈/日志；B1 → 功能 F1 示例说明）

### M5-2 窗口批 —— 先把"门窗"装好（真机验证最前）
- **目的（人话）**：外壳行为（无边框/三按钮/关窗进托盘/记住窗口位置）是后面每批都要碰的底层，先做先验证。
- **覆盖**：D4 + D7
- **产出**：主进程窗口改造（frame:false + Tray + bounds 持久化 + close 时序）、preload window.controls、渲染层三按钮（最小化/最大化/关闭）、托盘菜单（打开/退出，品牌 logo）
- **验收（本机 `cd project && pnpm dev`）**：
  1. 窗口无系统标题栏，顶部空白可拖动
  2. 三按钮：最小化→任务栏；最大化↔还原；关闭→隐藏进托盘（任务栏消失）
  3. 托盘右键：「打开」恢复窗口；「退出」进程完全结束
  4. 调窗口大小/位置 → 关闭 → 重启 → 保持
  5. 编辑后直接关窗 → 重启 → 内容已保存（磁盘 JSON 最新值）
  6. 导出 PDF 正常（打印窗口不受影响）
  7. Win10 机器重复 1–4（圆角/阴影异常 → 报告，走 titleBarOverlay 回退）
- **风险**：Win10 圆角异常（预警：标题栏发方/无阴影）；托盘图标消失（预警：GC——模块级持引用）；关窗保存中断（预警：重启后内容丢失）
- **边界（不做）**：窗口动画/自绘阴影/多实例限制；托盘不加「新建简历」菜单项（已拍板）
- **治理**：三文档同步（技术栈 §3.15.1 窗口按钮"未建待 M5"→已建；实现情况；功能 F11）；**E1 真机验证本批完成后即可执行**（不必等 M5-7）

### M5-3 模板参数化 —— 把"固定配方"变"可调配方"
- **目的（人话）**：模板参数从代码常量变用户可改配置：改参数→保存→用这套模板的简历跟着变，还原一键恢复。
- **覆盖**：A3 + A4 + A7
- **产出**：settings.ts templates 覆盖层实现 + layout.ts lv() 消费改造 + 模板编辑 UI + 字体覆盖链（模板默认 vs 本简历）
- **验收（本机）**：
  1. 设置→模板设置→选 classic→行高 1.8→2.2→保存 → 预览行距变宽
  2. 打开已有简历（未单独调排版）→ 行距同样变宽（全局生效）
  3. 模板设置「还原」→ 预览恢复初始；已有简历恢复原样
  4. 某简历 LayoutBar 单独改过字体 → 模板改字体不影响该简历（覆盖链生效）
  5. 导出 PDF 与预览一致
  6. `npm run typecheck && npx vitest run` 全绿
- **风险**：覆盖链写反（per-resume 被全局误覆盖——预警：单独调过的简历跟全局变了）；模板=打印漂移（预警：导出≠预览）；还原只清模板层不清 per-resume（边界写明）
- **边界（不做）**：模板参数不涉内容结构（section 排序仍 per-resume）；不引入新模板
- **治理**：A3 契约已 F2 冻结 ✅；三文档同步（功能 F4 / 技术栈 §3.7 / 介绍 §四）

### M5-4 设置屏 + 字体 —— "控制面板"三页签 + 字体系统
- **目的（人话）**：外观（颜色/语言）、存储位置（数据放哪）、字体（导入自用）三个设置屏 + 字体全链路，用户第一次能个性化应用。
- **覆盖**：D1 + D2 + D3 + D5
- **产出**：三个设置屏 UI + 字体协议/枚举/导入 + theme/derive.ts 接线
- **验收（本机）**：
  1. 外观：4 色单选，dark→全界面变深，300ms 内完成
  2. 「跟随系统」+ 系统切深色 → 应用跟随
  3. 语言 English → 全 UI 英文（简历内容不变）
  4. 存储：显示当前路径；「更改位置」→ 提示"已迁移 N 份"→ 旧文件保留；「重置」二次确认回默认
  5. 导入 .ttf → 许可提示 → 字体入列表 → 简历字体可选 → 预览生效
  6. 导出 PDF 用导入字体正常（无乱码/缺字）
- **风险**：queryLocalFonts 权限（预警：渲染层拿不到字体列表 → Electron session permission handler 放行）；导入字体导出未嵌入（预警：PDF 缺字）；主题切换 FOUC（预警：白闪）
- **边界（不做）**：自定义主题不做选色器 UI（derive 派生即可）；字体不做打包分发（仅本机）
- **治理**：D5 依赖 F2 契约 ✅；三文档同步（技术栈 §3.7.4/§3.11.5/§3.15/§3.15.1.x；功能 F18/F21/F13）

### M5-5 模板设置主功能 —— "模板橱窗"开门营业
- **目的（人话）**：组装成完整功能：设置第 4 页签（橱窗+配方编辑入口）、新建先挑模板、默认模板记忆。
- **覆盖**：A1 + A2 + A5 + A6
- **产出**：设置路由/navSub/卡片 + 模板预览屏 + NewResumeView 改造 + 默认模板持久化
- **验收（本机）**：
  1. 设置主页第 4 张卡「模板设置」，样式一致；左侧导航设置第 4 子项
  2. 进入：三模板并排，各用新示例数据渲染；点选→预览切换
  3. 点「编辑」→ 改参数 → 预览不实时变；「保存」→ 预览更新
  4. 新建→空白→先出模板选择（三卡缩略图）→ 选 modern → 新简历即 modern
  5. 默认模板设 modern → 新建时预选
  6. 导入 PDF → 三步向导最后一步模板选择 → 选 compact → 导入结果 compact 渲染
- **风险**：预览示例数据与真实简历混淆（预警：模板预览出现用户真实内容=数据源 bug）；新建漏选默认（预警：新建出来不是默认模板）
- **边界（不做）**：模板市场/上传（v2）；模板设置不动 LayoutBar（per-resume 编辑保留）
- **治理**：A6 契约已 F2 冻结 ✅；三文档同步（功能 F4/F6/F11；技术栈 §3.7；介绍 §四）

### M5-6 导入向导美化 + 收尾 —— 打磨最后环节
- **目的（人话）**：导入体验（并排对照/AI 标记/风格统一）+ 日志 + 关于页 + 错误提示收尾。
- **覆盖**：C1–C4 + A8 + D6 + D8 + D9
- **产出**：ImportWizard 三步界面重构 + 模板选择插入 + 日志系统 + 关于页
- **验收（本机）**：
  1. 导入单份 PDF：①步左原始/右解析并排；②步字段可改 + AI 来源标记；③步模板选择 + 新建/覆盖二次确认
  2. 批量导入：弹窗选模板 → 逐份落盘 → 结果摘要（成功 N/失败清单）
  3. `userData/logs/app.log` 存在有启动记录；导出日志按钮 → zip
  4. 设置→关于：版本号 + 开源链接 + 导出日志
  5. 故意导出失败 → 中文错误提示
  6. `npm run typecheck && npx vitest run` 全绿
- **风险**：向导重构破坏三步流程（预警：步骤跳转失效/写入无二次确认）；electron-log ESM/轮转异常（预警：日志不写入/不轮转）；C2 若需 ImportMap 加来源标记字段 → 追加契约审批（优先视觉提示实现不碰 schema）
- **边界（不做）**：不做导入去向导重构（已撤回）；不改批量导入免核对语义（R8 定案）
- **治理**：⚠️ D6 过 G.2（调研已通过：零依赖/MIT/93KB/ESM OK）；C2 若碰 schema 需审批；三文档同步（功能 F6/F11/F14；技术栈 §3.11.1/§3.8）

### M5-7 真机总验证 —— 全面体检
- **目的（人话）**：所有功能真机完整走一遍出验收报告，顺手清卫生问题。
- **覆盖**：E2 + E3 + E4 + D10
- **验收（本机）**：
  1. `cd project && XM_UI_SMOKE=1 pnpm dev` → UI_SMOKE_RESULT JSON 全过、退出码 0
  2. `XM_EXPORT_SMOKE=1` 同
  3. 导出 PDF vs `material/简历示例1.pdf`：条目头职位主行/技能行分类前缀/标题工作经验/日期连字符符合预期
  4. 隐私模式导出遮罩与预览一致（O2）
  5. 联系信息 2 列定论（8 项数据时）
  6. 照片引用加载无闪烁；新建视图居中
  7. axe 复扫 2 moderate 清零
- **风险**：真机环境差异（缩放/多显示器/Defender）；任一不通过 → 回对应批次修（M5-2~6 有回退预案）
- **边界（不做）**：无新功能，仅验证与修复
- **治理**：G.7 全量自检 + 全部批次文档同步最终核对 + 日志收口

## 四、治理节点汇总

| 节点 | 位置 | 动作 |
|---|---|---|
| ⚠️ 需审批 | M5-1 F2 契约冻结（templates/defaultTemplateId/font IPC/logs:export）；M5-6 C2 若碰 ImportMap schema | 集成者批准 |
| ✅ 过 G.2 | M5-6 D6 electron-log | 调研已通过（零依赖/MIT/93KB/ESM OK） |
| 三文档同步 | 每批收口列明（M5-1 F1/B1；M5-2 技术栈§3.15.1；M5-3 功能 F4/技术栈§3.7；M5-4 功能 F18/F21/技术栈§3.7.4/3.11.5；M5-5 功能 F4/F6/F11；M5-6 功能 F6/F11/技术栈§3.11.1） | 同批 commit |

## 五、待确认假设点（用户确认后开工）

1. **A3/A4 可编辑模板参数清单**：6 排版数值（字号/行高/页边距/段落间距/节间距/节标题字号）+ 模板默认字体 + 主题色 + 节标题风格（下划线/色条/紧凑三选一）
2. **A4 编辑界面形态**：模板设置屏内——左侧模板列表 + 右侧编辑面板，点「保存」后预览才更新
3. **批次顺序**：窗口批 M5-2 排最前（用户要求真机验证最前），模板参数化 M5-3，设置屏 M5-4，模板设置屏 M5-5，向导+收尾 M5-6，真机总验证 M5-7

## 六、用户旅程验收清单（G.3 复盘基准）

> 收口复盘先对照此清单查"用户能否走通"，而非"清单是否勾完"。

1. 用户打开应用 → 无系统标题栏，顶部可拖动，三按钮可用
2. 关窗 → 进托盘 → 托盘「打开」恢复、「退出」完全退出；重启窗口位置保持
3. 设置 → 模板设置：三模板预览（新示例数据）→ 编辑 classic 行高 → 保存 → 预览变宽 → 打开已有简历跟随 → 还原恢复
4. 新建 → 空白 → 选模板 → 新简历即所选模板；默认模板记忆生效
5. 导入 PDF → 三步向导：并排对照 / AI 标记 / 最后选模板 → 导入结果用所选模板
6. 设置 → 外观：切 4 色 300ms 生效 / 跟随系统 / 语言切换
7. 设置 → 存储：改位置迁移 N 份 / 重置回默认
8. 导入字体 → 许可提示 → 简历字体可用 → 导出 PDF 正常
9. 设置 → 关于：版本 / 开源链接 / 导出日志
10. 导出失败 → 中文错误提示
11. 日志文件存在且轮转
12. 隐私模式导出遮罩一致（O2）；联系信息 2 列；照片无闪烁
