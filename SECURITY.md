# Security Policy

## 安全披露 / Reporting a Vulnerability

**xiaomengresume 重视安全性。** 如果你发现安全漏洞，请通过以下方式负责任地披露，不要公开提交 Issue。

### 联系方式

- **Email**：请将漏洞报告发送至项目维护者邮箱（将在首次 Release 时公布）
- **PGP 密钥**：可选，用于加密通信（将在首次 Release 时公布）

我们承诺：

- **48 小时内**确认收到报告
- **7 天内**给出初步评估和修复计划
- 修复后与报告者协调披露时间

---

## 安全承诺

### 数据主权

**所有数据存本机，代码开源可审计。**

- 简历数据以 JSON 文件形式存储在用户本地磁盘
- 应用不设服务器、不设账号体系、不收集遥测数据
- 应用仅在以下场景联网：
  - AI 功能（BYOK 模式下，用户自行配置 API Key 后，请求直接发送至用户指定的 AI 供应商）
  - 检查更新（可选，仅启动时静默检查或设置页手动检查）
  - 安装时的依赖下载

### API Key 加密实现

AI 功能使用 BYOK（Bring Your Own Key）模式，用户自行配置 API Key。Key 的存储遵循以下安全设计：

| 层级 | 实现 | 说明 |
|------|------|------|
| 存储 | `electron-store` + `safeStorage` | 加密后写入本地磁盘，密钥由操作系统密钥链管理（Windows DPAPI / macOS Keychain / Linux libsecret） |
| 传输 | 仅存在于主进程内存 | API Key 永不落入渲染进程，前端无法通过任何方式读取到 Key 原文 |
| 加密算法 | AES-256-GCM（经 safeStorage 加密） | 操作系统级加密，密钥不落盘 |
| Linux 降级 | 若 `safeStorage.isEncryptionAvailable()` 返回 false | 方案 A（推荐）：退化为 electron-store 弱加密 + 一次性告警提示；方案 B：拒绝存储 Key，每次启动手动输入 |

### 无遥测声明

**本项目不收集任何遥测数据、使用统计或错误报告。** 应用不会在后台发送任何网络请求，除非用户主动使用以下功能：

1. AI 辅助编辑（请求发送至用户自行配置的 AI 供应商端点）
2. 检查更新（请求发送至 GitHub Releases API）

### 依赖安全

- 依赖缓存策略见《技术栈.md》，所有依赖在首次构建时锁定版本
- 依赖引入需经过 G.2 三问检查（无原生编译 / 无额外二进制 / 非 2 年未发版）
- 定期使用 `pnpm audit` 检查已知漏洞

---

## 安全更新

安全更新将通过 GitHub Releases 发布，并遵循以下原则：

- 严重漏洞：**7 天内**发布修复版本
- 中危漏洞：**30 天内**或随下一个常规版本发布
- 低危漏洞：随下一个常规版本发布

---

## 安全相关配置

- 自动更新：`electron-updater` + GitHub Releases，仅检查更新时联网
- 隐私打码模式：一键脱敏联系方式，放心分享简历截图

---

**我们相信开源安全——代码可审计是最好的安全承诺。**
---

## 凭据与敏感信息保护（2026-08-11）

- **凭据不落源码**：API Key / 令牌 / 密钥只从环境变量或密钥服务读取（应用运行时 = safeStorage keyring + 主进程环境变量）；源码、示例、测试不得写入可用凭据字面量（占位符如 sk-... 除外）。
- **禁本机绝对路径**：文档/代码/示例禁写本机绝对路径（泄露用户名与目录结构）；需提路径用通用描述（<用户目录> 等）。
- **CI 级持续扫描**：`.github/workflows/security.yml` 每次 push/PR 跑 gitleaks（密钥/令牌/私钥正则扫描，命中即失败）。
- **GitHub Secret scanning**：请维护者在仓库 Settings → Code security and analysis → Secret scanning 启用（GitHub 原生扫描已推送历史与未来提交的已知密钥格式）。
