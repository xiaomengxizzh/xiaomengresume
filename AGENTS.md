# xiaomengresume · 项目指令（AGENTS.md）

> 本文件为 ZCode 自动加载的项目级指令（与用户级 `~/.zcode/AGENTS.md` 合并生效），
> 内容为**脱敏精简版记忆**（公开 GitHub 仓库安全）。完整记忆已迁移至
> ZCode 持久化记忆 `~/.zcode/cli/memories/projects/xiaomengresume-*/memory/`（自动加载）。

## 项目本质
- 开源免费（MIT）· 隐私优先 · 防盲信 AI 的桌面简历编辑器（Electron + React + TS）。
- AI 仅辅助编辑，**禁"一键生成简历"**；数据全本机、无账号、无订阅、无水印。

## 文档体系（唯一真相源在 `file/`）
- **6 主文档**：项目介绍（是什么）/ 项目规范（怎么做才对）/ 技术栈（怎么做）/ 项目功能（做什么）/ 项目实现情况（做到哪）/ 项目日志（改了什么）。
- **路由化定案**：主文档承载 路由表 + 摘要 + 铁律常量；细节下沉 `file/detail/`（`functions/` 按 F 编号 · `logs/` 按月 · `api/` 脚本自动生成**禁手写**）；`plans/` 里程碑计划长期存档；`rubbish/` 可清理。
- 查找任何内容：先读主文档索引 → 按路由表翻子文档。

## 项目维护规则（AI 自动执行，无需用户提醒）
1. **三文档同步铁律（规范 §1.3.1）**：新增/变更若超出《项目介绍》《项目功能》《技术栈》任一文档描述范围（新功能 / 新字段 / IPC 契约 / 新依赖 / 架构决策 / 模板·导出行为 / 交互形态）→ 必须同步更新对应文档 + 开头索引表，与代码/日志**同批 commit**，禁「代码先行、文档滞后」；仅修既有行为且不改变描述可豁免，但须在 G.7 自检 B 显式标注「三文档无需动」及理由。
2. **日志归口（§1.4）**：改任何文档/代码/定决策，必先登记《项目日志.md》当日条目（月度索引表 + `file/detail/logs/YYYY-MM.md` 条目**同步更新**，主文档与子文档同步提交）。
3. **状态落点**：代码/功能改动 → 《项目实现情况.md》§一「已实现」补条目；有取舍/与定案差异 → §2.4 偏差登记补新编号。
4. **收口自检（G.7）**：B 文档同步四连（日志 / 实现情况 / 偏差 / 关联文档扫描）逐项确认；必跑 `project/../scripts/docs-tool.py selfcheck`（CH7 实现情况覆盖 = 最近 commit 改动必须在《项目实现情况》有对应条目）。
5. **material 示例对比规则（2026-08-09 用户定）**：每次功能收口后，对照 `material/` 示例材料（`简历示例1.json`/`.pdf`/`_1.png`/`_2.png` + `UI示例_参考图.html` + `项目导出简历示例.json`/`.pdf`）逐项对比布局/样式/内容结构/交互，输出差异分析 + 改进清单（优先用户体验/视觉一致性/功能完整性）；需要像素级复核时调用 doubao 视觉工具（`node ~/.agents/scripts/vision-analyze.mjs <图> "<问题>"`）。注意 `简历示例1.json` 为旧版 schema，结构基准 = F1 对齐的 `project/src/main/files/sample-resume.json`，material 用于视觉/交互基准。

## 关键铁律速查
- **AI 定位铁律**：AI 辅助编辑（语法/润色/匹配打分），禁一键生成简历、只改已有内容。
- **隐私（F16）**：隐私打码按「显示位置」全打码（预览 CSS + PDF 导出一致）；数据全本机。
- **配色**：4 色令牌唯一权威《技术栈.md》§3.15.1 + F18（4 权威 + 2 派生）；防绿字护栏；`--resume-*` 与主题令牌正交。
- **契约冻结**：`src/shared/`（Zod schema + IPC 通道）变更须经集成者批准；API 文档禁手写（`project/scripts/gen_api_docs.mjs` 生成，变更后必重跑）。
- **文案**：禁硬编码中文，走 F13 i18n（zh/en 对称，新 key 双端同步）。
- **依赖**：新依赖过 G.2 三问（原生编译？额外二进制？超 2 年未发版？）。
- **git**：收口后 AI 自行 commit（规范 message，禁 --no-verify）；**开源仓库**（github.com/xiaomengxizzh/xiaomengresume）push 前先 `git fetch` 检查远端外部改动；`project/used_keys.txt` 类疑似敏感文件**永不提交**。
- **凭据与隐私（2026-08-11）**：凭据只从环境变量/密钥服务读取，源码/示例/测试禁写可用凭据字面量（占位符 sk-... 可）；**禁写本机绝对路径**（文档/代码，泄露用户名与目录结构，需提路径用 `<用户目录>` 等通用描述）；CI 跑 gitleaks（security.yml），GitHub Secret scanning 已建议启用（SECURITY.md）。
- **性能与结构铁律（2026-08-11，规范五章 8-13）**：主进程重依赖（unpdf/mammoth/@ai-sdk/pdf-lib）一律函数内动态 import；渲染低频视图一律 lazy 拆 chunk（编辑区 TipTap 保持同步）；大对象（base64 图 >100KB）出 JSON 转文件+引用；隐藏窗口用完即毁 + backgroundThrottling:false；新增功能过渲染/内存自检 5 问（编辑器实例数/全量拷贝/列表扫描/监听清理/快照就绪）；性能改动先量化基线再优化。
- **验证**：改动收口必过 typecheck + lint（--max-warnings 0）+ vitest；涉及持久化/自动保存/启动恢复/路由跳转须 `XM_UI_SMOKE=1` 通过。

## 长期记忆深读指引
- 完整记忆在 ZCode 持久化记忆 `~/.zcode/cli/memories/projects/xiaomengresume-*/memory/`（自动加载，含环境操作细节；项目内 `.workbuddy/` 已移除，2026-08-08 重构）：
  - `MEMORY.md` — 速查索引（项目本质/文档工具/维护规则/配色/界面/待拍板/环境/里程碑）
  - `REFERENCE.md` — 详细档案（界面定案全参、配色令牌全文、环境操作备忘全文）
  - `2026-08-*.md` — 按日记忆
- 需要项目状态、环境细节、历史决策时主动读对应文件；本 AGENTS.md 只承载公开安全的核心规则。
